# ComfyEndpoints Contract Plugin

This plugin provides `ApiInput` and `ApiOutput` nodes used to declare request/response contracts for ComfyEndpoints.

## Exporting a contract

Use `export_contract_from_workflow(workflow_path, output_path)` to generate `workflow.contract.json`.
